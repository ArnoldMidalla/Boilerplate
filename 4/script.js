// window.addEventListener('scroll', function () {
//     const header = document.querySelector('.header');
//     if (window.scrollY > 120) { 
//         header.classList.add('scrolled');
//     } else {
//         header.classList.remove('scrolled');
//     }
// });

window.addEventListener('scroll', function () {
    const header = document.querySelector('.header');
    const scrollY = window.scrollY;

    if (scrollY > 100 && scrollY <1240) { // You can change the scroll distance as needed
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else if (scrollY > 1240 && scrollY <1807) { 
        header.classList.add('scrolledB');
        header.classList.remove('scrolled');
    } else if (scrollY > 1807 && scrollY <2630) {
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else {
        header.classList.remove('scrolled', 'scrolledB');
    }
});

// window.addEventListener('scroll', function () {
//     const header = document.querySelector('.butcent');
//     if (window.scrollY > 120) { // You can change the scroll distance as needed
//         header.classList.add('scrolled');
//     } else {
//         header.classList.remove('scrolled');
//     }
// });

window.addEventListener('scroll', function () {
    const header = document.getElementById('js1'); // Use ID selector
    if (scrollY > 100 && scrollY <1240) { // You can change the scroll distance as needed
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else if (scrollY > 1240 && scrollY <1807) { 
        header.classList.add('scrolledB');
        header.classList.remove('scrolled');
    } else if (scrollY > 1807 && scrollY <2630) {
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else {
        header.classList.remove('scrolled', 'scrolledB');
    }
});

window.addEventListener('scroll', function () {
    const header = document.getElementById('js1'); // Use ID selector
    if (scrollY > 100 && scrollY <1240) { // You can change the scroll distance as needed
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else if (scrollY > 1240 && scrollY <1807) { 
        header.classList.add('scrolledB');
        header.classList.remove('scrolled');
    } else if (scrollY > 1807 && scrollY <2630) {
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else {
        header.classList.remove('scrolled', 'scrolledB');
    }
});

window.addEventListener('scroll', function () {
    const header = document.getElementById('js2'); // Use ID selector
    if (scrollY > 100 && scrollY <1240) { // You can change the scroll distance as needed
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else if (scrollY > 1240 && scrollY <1807) { 
        header.classList.add('scrolledB');
        header.classList.remove('scrolled');
    } else if (scrollY > 1807 && scrollY <2630) {
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else {
        header.classList.remove('scrolled', 'scrolledB');
    }
});

window.addEventListener('scroll', function () {
    const header = document.getElementById('js3'); // Use ID selector
    if (scrollY > 100 && scrollY <1240) { // You can change the scroll distance as needed
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else if (scrollY > 1240 && scrollY <1807) { 
        header.classList.add('scrolledB');
        header.classList.remove('scrolled');
    } else if (scrollY > 1807 && scrollY <2630) {
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else {
        header.classList.remove('scrolled', 'scrolledB');
    }
});

window.addEventListener('scroll', function () {
    const header = document.getElementById('js4'); // Use ID selector
    if (scrollY > 100 && scrollY <1240) { // You can change the scroll distance as needed
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else if (scrollY > 1240 && scrollY <1807) { 
        header.classList.add('scrolledB');
        header.classList.remove('scrolled');
    } else if (scrollY > 1807 && scrollY <2630) {
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else {
        header.classList.remove('scrolled', 'scrolledB');
    }
});

window.addEventListener('scroll', function () {
    const header = document.getElementById('js5'); // Use ID selector
    if (scrollY > 100 && scrollY <1240) { // You can change the scroll distance as needed
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else if (scrollY > 1240 && scrollY <1807) { 
        header.classList.add('scrolledB');
        header.classList.remove('scrolled');
    } else if (scrollY > 1807 && scrollY <2630) {
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else {
        header.classList.remove('scrolled', 'scrolledB');
    }
});

window.addEventListener('scroll', function () {
    const header = document.getElementById('js6'); // Use ID selector
    if (scrollY > 100 && scrollY <1240) { // You can change the scroll distance as needed
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else if (scrollY > 1240 && scrollY <1807) { 
        header.classList.add('scrolledB');
        header.classList.remove('scrolled');
    } else if (scrollY > 1807 && scrollY <2630) {
        header.classList.add('scrolled');
        header.classList.remove('scrolledB');
    } else {
        header.classList.remove('scrolled', 'scrolledB');
    }
});